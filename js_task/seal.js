let obj={
    name:"botttle"
   
}
Object.seal(obj)//can't delete obj seal & freeze//can't add the properties seal & freeze//upadate the properties//can't update existing properties.
console.log(obj)
